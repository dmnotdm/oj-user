spring:
  application:
    name: oj-user-service
  datasource:
    username: root
    password:
    url: jdbc:mysql://host.ip:3306/online_judge?characterEncoding=UTF-8
server:
  port: 9001
eureka:
  client:
    service-url:
      defaultZone: http://host.ip:8000/eureka/
#management:
#  endpoints:
#    web:
#      exposure:
#        include: ["health","info","shutdown"]
#  endpoint:
#    shutdown:
#      enabled: true