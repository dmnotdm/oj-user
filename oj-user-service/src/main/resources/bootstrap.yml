spring:
  application:
    name: oj-user-service
  datasource:
    username: root
    password:
    url: jdbc:mysql://host.ip:3306/cmdtalk?characterEncoding=UTF-8
server:
  port: 0
#  servlet:
#    context-path: /user
eureka:
  client:
    service-url:
      defaultZone: http://host.ip:8000/eureka/